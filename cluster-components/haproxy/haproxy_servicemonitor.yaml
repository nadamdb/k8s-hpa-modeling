apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: haproxy
  namespace: monitoring
spec:
  selector:
    matchLabels:
      run: haproxy-ingress
  endpoints:
  - port: stat
    interval: 5s
